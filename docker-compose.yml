services:
  api:
    build: . 
    container_name: task_manager_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on: db 

  db:
    image: postgres:16
    container_name: task_manager
    env_file:
      - .env
    environment:
      - DATABASE_URL=

volumes:
